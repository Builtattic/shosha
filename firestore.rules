rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Local and production app access goes through firebase-admin on the server.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
